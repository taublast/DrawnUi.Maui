<?xml version="1.0" encoding="utf-8" ?>
<views:BasePageCodeBehind
    x:Class="Sandbox.Views.MainGC"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:DrawnUi.Maui.Demo.Views.Controls"
    xmlns:controls1="clr-namespace:SkiaSharp.Views.Maui.Controls;assembly=SkiaSharp.Views.Maui.Controls"
    xmlns:demo="clr-namespace:Sandbox"
    xmlns:draw="http://schemas.appomobi.com/drawnUi/2023/draw"
    xmlns:drawnUi="clr-namespace:DrawnUi.Maui;assembly=DrawnUi.Maui"
    xmlns:gestures="clr-namespace:AppoMobi.Maui.Gestures;assembly=AppoMobi.Maui.Gestures"
    xmlns:partials="clr-namespace:Reversi.Views.Partials"
    xmlns:system="clr-namespace:System;assembly=System.Runtime"
    xmlns:views="clr-namespace:Sandbox.Views"
    x:Name="ThisPage"
    x:DataType="demo:MainPageViewModel"
    BackgroundColor="Transparent">


    <!--<controls1:SKGLView
        HorizontalOptions="Fill"
        HasRenderLoop="True"
        VerticalOptions="Fill"
        PaintSurface="OnPaintSurface"
        EnableTouchEvents="False"  />-->

    <draw:DrawnView
        UpdateMode="Constant"
        BackgroundColor="Blue"
        HardwareAcceleration="Enabled"
        HorizontalOptions="Fill"
        Tag="MainPage"
        VerticalOptions="Fill"/>

    <!--<draw:DrawnView
        UpdateMode="Dynamic"
        x:Name="MainCanvas"
        Margin="0,0,0,32"
        BackgroundColor="Blue"
        HardwareAcceleration="Enabled"
        HorizontalOptions="Fill"
        Tag="MainPage"
        VerticalOptions="Fill">

        <draw:SkiaLayout
            BackgroundColor="Red"
            HorizontalOptions="Fill"
            AddMarginTop="24"
            VerticalOptions="Fill">

            <partials:ScreenFun
                HorizontalOptions="Fill"
                VerticalOptions="Fill" />

            <draw:SkiaLabelFps
                Margin="0,0,4,84"
                BackgroundColor="DarkRed"
                ForceRefresh="False"
                HorizontalOptions="End"
                Rotation="-45"
                TextColor="White"
                VerticalOptions="End" />


        </draw:SkiaLayout>


    </draw:DrawnView>-->

</views:BasePageCodeBehind>
